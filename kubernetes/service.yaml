apiVersion: v1
kind: Service
metadata:
  name: ink-playground-service
spec:
  selector:
    app: ink-playground-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
  type: NodePort
