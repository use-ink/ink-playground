[package]
name = "generate-bindings"
version = "0.1.0"
edition = "2021"
authors = ["Achim Schneider <achim@parity.io>"]
license = "Apache-2.0"
readme = "README.md"

[dependencies]
clap = "3.1.18"
ts-rs = { version = "6.1.2", features = [
    "serde-compat",
] }

backend = { path = "../backend" }
